InstallTrace 0.2.0 — Midnight Console refresh for Windows x64.

- Completely redesigned desktop interface with a deeper dark theme and clearer visual hierarchy.
- New scan-scope panel, capture-profile controls, three-step snapshot cards and live state chips.
- Redesigned primary/secondary/danger buttons with hover feedback and stronger spacing.
- Cleaner comparison command bar, search/filter controls and a larger results workspace.
- Color-coded added/modified/removed/uncertain rows plus a dedicated JSON inspector panel.
- Improved status feedback for capture, demo data, snapshot coverage and comparison results.
- Core capture, comparison, leftover detection and export behavior remains read-only and unchanged.
- Includes SHA256SUMS.txt and executable smoke-test results.

Published only after Linux/Windows regression tests and a Windows executable smoke test pass.
The hosted build machine is GitHub's Windows runner. Consumer Windows 10/11 validation is still
recommended. This preview is unsigned and may trigger Windows SmartScreen. Check the
publisher/source and hash; InstallTrace is not an antivirus or installer-attribution engine.
